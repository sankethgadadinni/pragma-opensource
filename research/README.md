# Research Assets

This directory is for paper notes, local source dumps, and reproduction scratch material.

- `pragma_overview.md` is a plain-English summary of PRAGMA's problem statement, tasks, data, and differences from prior models.
- `paper_assets/` holds local copies of the PRAGMA paper source and tarball.
- `paper_assets/` is intentionally gitignored so the public repo stays focused on code.
